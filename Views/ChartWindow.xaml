<Window x:Class="GoldPriceMonitor.Views.ChartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        Title="价格趋势图" Height="400" Width="600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True">
    <Window.Resources>
        <ResourceDictionary Source="/Resources/Styles.xaml" />
    </Window.Resources>

    <Grid Margin="0,0,0,0">
        <Border Background="Transparent"
                BorderBrush="{StaticResource AccentColor}"
                BorderThickness="2"
                CornerRadius="12"
                Effect="{StaticResource DropShadow}">
            <Border Background="{StaticResource WindowBackground}" CornerRadius="10" Margin="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="40" />
                    </Grid.RowDefinitions>

                    <!-- 标题栏 -->
                    <Border Grid.Row="0" Background="{StaticResource HeaderBackground}" CornerRadius="10,10,0,0"
                            MouseLeftButtonDown="Window_MouseLeftButtonDown">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="📈 价格趋势" Foreground="White"
                                       VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="Bold"/>
                            <Button Grid.Column="1" Content="✕" Width="30" Height="30"
                                    Background="Transparent" Foreground="White" BorderThickness="0"
                                    Click="CloseButton_Click" />
                        </Grid>
                    </Border>

                    <!-- 图表区域 -->
                    <Grid Grid.Row="1" Margin="10">
                        <lvc:CartesianChart Series="{Binding Series}"
                                            XAxes="{Binding XAxes}"
                                            YAxes="{Binding YAxes}"
                                            Background="Transparent"
                                            LegendPosition="Top" />
                    </Grid>

                    <!-- 时间筛选按钮 -->
                    <Border Grid.Row="2" Background="{StaticResource FooterBackground}" CornerRadius="0,0,10,10">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
                                    VerticalAlignment="Center" Margin="0,5">
                            <RadioButton Content="1小时" GroupName="TimeRange" Margin="5,0"
                                         IsChecked="{Binding SelectedRange, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=OneHour}"
                                         Checked="TimeRange_Checked" Style="{StaticResource RadioButtonStyle}" />
                            <RadioButton Content="6小时" GroupName="TimeRange" Margin="5,0"
                                         IsChecked="{Binding SelectedRange, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=SixHours}"
                                         Checked="TimeRange_Checked" Style="{StaticResource RadioButtonStyle}" />
                            <RadioButton Content="24小时" GroupName="TimeRange" Margin="5,0"
                                         IsChecked="{Binding SelectedRange, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=TwentyFourHours}"
                                         Checked="TimeRange_Checked" Style="{StaticResource RadioButtonStyle}" />
                            <RadioButton Content="7天" GroupName="TimeRange" Margin="5,0"
                                         IsChecked="{Binding SelectedRange, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=SevenDays}"
                                         Checked="TimeRange_Checked" Style="{StaticResource RadioButtonStyle}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Window>
